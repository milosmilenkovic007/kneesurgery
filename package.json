{
  "name": "kneesurgery",
  "version": "1.0.0",
  "description": "Standalone WordPress theme for Knee Surgery Turkey",
  "scripts": {
    "build": "npm run build:assets",
    "build:assets": "node scripts/build-assets.mjs",
    "watch:assets": "node scripts/build-assets.mjs --watch",
    "clean:assets": "rm -rf dist"
  },
  "keywords": [
    "wordpress",
    "theme"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.10"
  },
  "dependencies": {
    "intl-tel-input": "^26.8.1"
  }
}
